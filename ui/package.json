{
  "name": "graph-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:graph": "node scripts/sync-graph.mjs",
    "dev": "npm run sync:graph && vite",
    "build": "npm run sync:graph && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@xyflow/react": "^12.8.6",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^5.4.19"
  }
}